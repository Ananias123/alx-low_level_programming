#include "main.h"

/**
 * _islower - a function that verify if the character is lowercase
 * @c: a character to verify
 *
 * Return: when c is lowercase return 1, otherwise return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');


}


