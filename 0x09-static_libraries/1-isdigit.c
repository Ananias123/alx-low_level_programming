#include "main.h"

/**
 * _isdigit - a function that returns True if all the characters are digits.
 * @c: integer to be checked
 * Return: return 1 if c is digit and return 0 if c is not a digit
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);


}
