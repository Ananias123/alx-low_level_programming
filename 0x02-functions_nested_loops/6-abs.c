#include "main.h"

/**
 * _abs - a function that prints the absolute value of a number
 * @k: the number to be checked
 * Return: k
 */
int _abs(int k)
{
	if (k < 0)
		return (-k);
	else
		return (k);
}
